{
	"dependencies": {
		"@types/prettier": "^2.7.1",
		"esbuild": "^0.16.3",
		"prettier": "^2.8.1",
		"typescript": "^4.9.4"
	},
	"private": true,
	"scripts": {
		"check": "tsc",
		"fmt": "prettier --write .",
		"build": "esbuild --minify --bundle --format=iife --global-name=mod --outfile=mod.js mod.ts"
	}
}
