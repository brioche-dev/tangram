{
	"dependencies": {
		"esbuild": "^0.18.0"
	},
	"name": "@tangramdotdev/global",
	"private": true,
	"scripts": {
		"build": "node build.js",
		"check": "tsc && eslint . && prettier --check .",
		"fmt": "prettier --write ."
	},
	"type": "module"
}
