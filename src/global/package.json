{
	"dependencies": {
		"esbuild": "^0.17.0",
		"prettier": "^2.8.1",
		"typescript": "^4.9.4"
	},
	"private": true,
	"scripts": {
		"check": "tsc",
		"fmt": "prettier --write .",
		"build": "esbuild --minify --bundle --format=iife --outfile=mod.js mod.ts"
	}
}
