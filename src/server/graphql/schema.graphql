scalar ExpressionJson
scalar ValueJson
scalar ReplJson

type Query {
  evaluate(expression: ExpressionJson!): ValueJson!
}

type Mutation {
  replNew: ReplJson!
  replRun(repl: ReplJson!, code: String!): String
}

schema {
  query: Query
  mutation: Mutation
}
