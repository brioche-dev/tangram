#!/usr/bin/env bash

export TANGRAM_BASHRC_INSTALLED=1

# Create the chpwd hook.
function tangram_chpwd {
  source <(tg autoshell hook bash)
}

# Install the hook.
export PROMPT_COMMAND="tangram_chpwd${PROMPT_COMMAND:+;$PROMPT_COMMAND}"

# Run the hook.
tangram_chpwd
